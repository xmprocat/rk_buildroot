config BR2_PACKAGE_LIBRETRO_STELLA2023
        bool "libretro-stella2023"
	depends on BR2_INSTALL_LIBSTDCPP
        help
          libretro stella2023 core.

	  http://www.libretro.com

comment "LIBRETRO_STELLA2023 needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
